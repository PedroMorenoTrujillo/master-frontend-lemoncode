import { ListSceneLayout } from "../layout";
import { PuppieContainer } from "../pods";

export const Puppie: React.FC = () => {
  return (
    <ListSceneLayout>
      <PuppieContainer />
    </ListSceneLayout>
  );
};
