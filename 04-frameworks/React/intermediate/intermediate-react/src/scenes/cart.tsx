import { CartContainer } from "../pods/cart";
import { CartLayout } from "../layout";
export const Cart: React.FC = () => {
  return (
    <CartLayout>
      <CartContainer />
    </CartLayout>
  );
};
